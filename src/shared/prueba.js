// const categoryTest = ["HTML", "CSS", "JAVASCRIT", "C", "KOTLIN", "PYTHON", "TYPESCRIPT"]

// const categories = (parans) => {
//    let result = parans.map(e => e)
//    console.log(result)
// } 

// categories(categoryTest)

// export default {categories}